# 🚀 Qwerty Learner - 本机运行说明

## 快速开始

### 方案一：使用自动化脚本（最简单）

1. 确保已安装 Node.js 18+
   - 下载地址：https://nodejs.org/
   - 安装后重启终端

2. 解压 `qwerty-learner-full.zip` 到 `C:\Users\pc\Desktop\treaQWER`

3. 双击运行 `运行项目.bat`
   - 脚本会自动：
     - 检查 Node.js 环境
     - 首次自动安装依赖
     - 启动开发服务器

4. 打开浏览器访问：`http://localhost:5173/`

---

### 方案二：手动操作

#### 1. 准备环境

确保已安装：
- **Node.js** (推荐 v18 LTS 或更高版本)
  - 下载：https://nodejs.org/
- **npm** (随 Node.js 一起安装) 或 **Yarn**

验证安装：
```cmd
node -v
npm -v
```

#### 2. 解压项目

将压缩包内容完整解压到目标文件夹：
```
C:\Users\pc\Desktop\treaQWER
```

#### 3. 安装依赖

打开 PowerShell 或 CMD，进入项目目录：
```cmd
cd C:\Users\pc\Desktop\treaQWER
```

安装依赖：
```cmd
# 使用 npm（推荐）
npm install

# 或使用 yarn（如果你已安装）
yarn install
```

#### 4. 启动项目

```cmd
# 使用 npm
npm run dev

# 或使用 yarn
yarn start
```

#### 5. 访问应用

浏览器会自动打开，或手动访问：
```
http://localhost:5173/
```

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` 或 `yarn start` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run lint` | 代码检查 |

---

## 常见问题

### Q: 端口被占用怎么办？
A: Vite 会自动选择其他端口，请查看终端显示的地址，例如 `http://localhost:5174/`

### Q: 安装依赖很慢？
A: 可以使用国内镜像源：
```cmd
npm config set registry https://registry.npmmirror.com
```

### Q: 找不到 node 命令？
A: 重启你的终端或电脑，确保 Node.js 安装正确并添加到 PATH 环境变量

---

## 项目结构

```
treaQWER/
├── src/                    # 源代码
│   ├── components/         # 通用组件
│   ├── pages/             # 页面组件
│   │   ├── Typing/        # 打字练习页面（含新的单词选择功能）
│   │   └── Gallery-N/     # 词库选择页面
│   ├── store/             # 全局状态
│   ├── utils/             # 工具函数
│   └── index.tsx          # 入口文件
├── public/                # 静态资源
├── package.json           # 项目配置
└── vite.config.ts         # Vite 配置
```

---

## 新功能：单词选择练习

本次修改新增了**从词典中选择指定单词练习**的功能：

### 使用方法
1. 点击"词库"
2. 选择任意词库
3. 在弹出的窗口中，点击 **"选择单词"** 标签
4. 搜索并勾选你想练习的单词
5. 点击 **"开始练习"**

### 相关文件
- [src/store/index.ts](file:///workspace/src/store/index.ts) - 新增自定义单词列表状态
- [src/pages/Typing/hooks/useWordList.ts](file:///workspace/src/pages/Typing/hooks/useWordList.ts) - 支持自定义单词模式
- [src/pages/Gallery-N/WordSelection/index.tsx](file:///workspace/src/pages/Gallery-N/WordSelection/index.tsx) - 单词选择组件
- [src/pages/Gallery-N/DictDetail/index.tsx](file:///workspace/src/pages/Gallery-N/DictDetail/index.tsx) - 词库详情页新增标签
- [src/pages/Typing/components/ResultScreen/index.tsx](file:///workspace/src/pages/Typing/components/ResultScreen/index.tsx) - 结果页面适配
- [src/pages/Typing/components/WordList/index.tsx](file:///workspace/src/pages/Typing/components/WordList/index.tsx) - 单词列表适配

---

## 技术栈

- **前端框架**: React 18 + TypeScript
- **状态管理**: Jotai
- **构建工具**: Vite
- **样式方案**: Tailwind CSS
- **路由**: React Router v6
- **数据库**: IndexedDB (Dexie.js)
